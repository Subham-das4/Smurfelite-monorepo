/**
 * Prisma-generated runtime (CJS). Import enums/models from here — not from `@smurfelite/types`
 * root (tsx/ESM cannot re-export CJS names from the package barrel).
 */
export * from "@smurfelite/types/src/generated/prisma/index.js";
